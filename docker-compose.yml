version: "3.8"

services:
  planilha-forex:
    image: nginx:alpine
    volumes:
      - ./Planilha_Gustavo_Pedrosa_FX.html:/usr/share/nginx/html/index.html:ro
    networks:
      - traefik_public
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      labels:
        - traefik.enable=true
        - traefik.http.routers.planilha-forex.rule=Host(`planilhaforex.testedev.online`)
        - traefik.http.routers.planilha-forex.entrypoints=websecure
        - traefik.http.services.planilha-forex.loadbalancer.server.port=80
        - traefik.http.routers.planilha-forex.service=planilha-forex
        - traefik.http.routers.planilha-forex.tls.certresolver=le

networks:
  traefik_public:
    external: true
    name: traefik_public
