version: "3.8"

services:
  planilha-forex:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./Planilha_code_pro.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped